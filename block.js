const checkEligibility = () => {
    let age = 23;
    if (age >= 18) {
        let name = "checking age";
        console.log("Eligible for voting ... ");
    } else {
        console.log("Not eligible for voting ... ");
    }
}

checkEligibility();


// There is a concept called GC


//  16 GB => RAM space 

// 1 GB data will be used 
// No more space to